// LocalStorage keys. Prefixed to avoid collisions with other apps on the same origin
export enum StorageKey {
    Token = 'cv.auth.token',
    User = 'cv.auth.user',
    Lockout = 'cv.auth.lockout',
}